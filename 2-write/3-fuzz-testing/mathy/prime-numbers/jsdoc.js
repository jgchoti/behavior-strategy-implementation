/**
 * Finds prime numbers up to a given number of steps on a staircase.
 * 
 * Prime numbers are numbers greater than 1 that have no divisors other than 1 and themselves.
 * 
 * @param {number} numberOfSteps The total number of steps on the staircase.
 * @returns {number[]} An array containing prime numbers up to the specified number of steps.
 */